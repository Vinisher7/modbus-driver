# ── MQTT Test Broker ──────────────────────────────────────────
# Configure these to point to your test/staging MQTT broker.
# The integration tests will skip gracefully if the broker is unreachable.

TEST_MQTT_BROKER="tcp://localhost:1883"
TEST_MQTT_CLIENT_ID="modbus_driver_test"
TEST_MQTT_USERNAME="admin"
TEST_MQTT_PASSWORD="Gr@fcet01"
TEST_TENANT_CODE="test-tenant"
